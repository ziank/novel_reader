<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:toolbarStyle">@style/AppTheme.ToolbarStyle</item>
        <item name="android:statusBarColor">@color/text_black</item>
        <item name="android:fastScrollThumbDrawable">@drawable/scroll_bar</item>
        <item name="android:fastScrollTrackDrawable">@color/transparent</item>
    </style>

    <!-- ToolBar -->
    <style name="AppTheme.ToolbarStyle" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@color/white_color</item>
    </style>
    <style name="Transparent" parent="Theme.AppCompat">
    　　
    　　<item name="android:windowBackground">@color/transparent</item>
    　　<item name="android:statusBarColor">@color/text_black</item>
    　　<item name="android:windowIsTranslucent">true</item>
    　　</style>

</resources>
